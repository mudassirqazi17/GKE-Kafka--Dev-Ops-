apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool
metadata:
  name: brokers
  namespace: kafka
  labels:
    strimzi.io/cluster: prod-kafka

spec:
  replicas: 3

  roles:
    - broker

  storage:
    type: persistent-claim
    size: 100Gi
    deleteClaim: false
